# YeYo API migration

Integrated:
- POST /scan
- GET /drivers/{driver_code}

Base URL:
https://qrcode.yeyocar.com/api

No Authorization header is sent because /scan was successfully tested without a token.

History API is intentionally not integrated.
